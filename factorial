numero = int(input('Digite um número para o factorial: '))
n = 0
n1 = 1
while n != numero:
    n = (n + 1)
    n1 = (n1 * n)
    print(n1)
    
    
    
'''
 from math import factorial
n = int(input('Digite um número para saber o fatorial: '))
f = factorial(n)
print ('O fatorial de {} é {}.' .format(n,f))

OBS: acima um comando pronto para cálculo do fatorial de um número.
'''
